#include <stdio.h>
#include <stdlib.h>

int main() {
    int number = 42;
    float pi = 3.14;
    
    if (number > 0) {
        printf("Positive number: %d\n", number);
    }
    
    return 0;
}